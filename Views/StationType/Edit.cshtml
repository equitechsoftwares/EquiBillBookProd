@{
    ViewBag.Title = Request.Cookies["data"].Value.Split('&')[8].Split('=')[1] + " | Edit Station Type";
    Layout = "~/Views/Shared/_CustomerLayout.cshtml";
}

<section class="content-header">
    <div class="container-fluid">
        <div class="row mb-2">
            <div class="col-sm-6">
                <h1>Edit Station Type</h1>
                <div class="float-left">
                    <ol class="breadcrumb float-sm-right">
                        <li class="breadcrumb-item"><a href="/dashboard/index">Dashboard</a></li>
                        <li class="breadcrumb-item"><a href="/settings/index">Settings</a></li>
                        <li class="breadcrumb-item"><a href="/stationtype/index">Station Types</a></li>
                        <li class="breadcrumb-item active">Edit Station Type</li>
                    </ol>
                </div>
            </div>
            <div class="col-md-6 col-12 d-flex align-items-center justify-content-start justify-content-md-end mt-1 mt-md-0">
                <a id="btnBack" href="/stationtype/index" class="btn btn-warning float-sm-right"><i class="fas fa-arrow-left"></i> Back</a>
            </div>
        </div>
    </div>
</section>

<section class="content">
    <div class="container-fluid">
        <div class="row">
            <div class="col-md-12">
                <div class="card card-primary card-outline">
                    <form id="stationTypeForm">
                        <input type="hidden" id="StationTypeId" name="StationTypeId" value="@(ViewBag.StationType != null ? ViewBag.StationType.StationTypeId : 0)" />
                        <div class="card-body">
                            <div class="row">
                                <div class="col-12 col-sm-6 col-md-4 col-lg-3">
                                    <div class="form-group">
                                        <label>Station Type Name <span class="text-danger">*</span></label>
                                        <input type="text" class="form-control divStationTypeName_ctrl" id="txtStationTypeName" name="StationTypeName" value="@(ViewBag.StationType != null ? ViewBag.StationType.StationTypeName : "")">
                                        <small class="text-red font-weight-bold errorText" id="divStationTypeName" style="display: none;"></small>
                                    </div>
                                </div>
                                <div class="col-12 col-sm-6 col-md-4 col-lg-3">
                                    <div class="form-group">
                                        <label>Description</label>
                                        <textarea class="form-control divDescription_ctrl" id="txtDescription" name="Description" rows="3">@(ViewBag.StationType != null ? ViewBag.StationType.Description : "")</textarea>
                                        <small class="text-red font-weight-bold errorText" id="divDescription" style="display: none;"></small>
                                    </div>
                                </div>
                            </div>
                        </div>
                        <div class="card-footer">
                            <button id="btnUpdate" type="button" class="btn btn-primary float-right" onclick="updateStationType(1)"><i class="fas fa-edit"></i> Update</button>
                            <a id="btnBack" href="/stationtype/index" class="btn btn-default float-right mr-2">Cancel</a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</section>

<script src="~/Content/JQuery/Customer/Restaurant/StationType.js"></script>

